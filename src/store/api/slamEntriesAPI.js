import { API_ORIGIN_URL } from "../../config";
import { store } from "../index";
import { updateEntriesInStore } from "../slamEntries/actions/slamEntries.actions";
export const addEntryAPI = (name, custom_bg, message, isLoggedIn) => {
  // call api
  //
  //
  //
  //
  // fetchAllEntries(isLoggedIn)
};
export const deleteEntryAPI = (_id, isLoggedIn) => {
  // api call
  //
  //
  //
  //
  // fetchAllEntries(isLoggedIn)
};
export const updateEntryAPI = (_id, isLoggedIn) => {
  // api call
  //
  //
  //
  //
  // fetchAllEntries(isLoggedIn)
};
export const fetchAllEntries = (isLoggedIn) => {
  // api call
  //
  //
  //
  //
  // store.dispatch(updateEntriesInStore(data))
};
